# Data Retention

Query logs are retained for **90 days** in the application database.

Exported CSV files in user workspaces are kept until the user deletes them.

Audit logs for enterprise tenants are retained for **7 years** per compliance requirements.

Personal data deletion requests are honored within 30 days per GDPR.
